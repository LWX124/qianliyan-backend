<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.app.dao.BizWxUserDao">
	
	<!-- 查询数据
	<select id="findList" resultType="BizWxUser">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->

	<!--BizWxUser findByOpenid(String openid);-->
	<select id="findByOpenid" resultType="BizWxUser">
		SELECT * FROM biz_wx_user WHERE openid = #{openid};
	</select>
	
</mapper>