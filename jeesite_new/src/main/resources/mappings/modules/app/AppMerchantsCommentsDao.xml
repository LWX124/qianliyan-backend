<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.app.dao.AppMerchantsCommentsDao">
	
	<!-- 查询数据
	<select id="findList" resultType="AppMerchantsComments">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->

	<select id="findAll" resultType="AppMerchantsComments">
		SELECT a.*,b.`name`,c.`name` FROM app_merchants_comments as a
			JOIN app_merchants as b on a.merchants_id =b.id
			JOIN app_user AS c ON a.user_id = c.id
			WHERE merchants_id = #{id,jdbcType=BIGINT};
	</select>

	<select id="findByCount" resultType="AppMerchantsComments">
		SELECT * FROM app_merchants_comments AS a
			WHERE content= #{CONTENT,jdbcType=VARCHAR}
	</select>
</mapper>