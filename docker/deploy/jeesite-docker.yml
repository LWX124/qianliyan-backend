
jdbc:
  type: mysql
  driver: com.mysql.jdbc.Driver
  url: jdbc:mysql://mysql:3306/jeesite?useSSL=false&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull
  username: root
  password: root123
  testSql: SELECT 1

spring:
  redis:
    host: redis
    port: 6379
    ssl: false
    database: 0
    password: ""
    timeout: 3000
    jedis:
      pool:
        max-active: 50
        max-wait: 5000
        max-idle: 50
        min-idle: 0
      timeout: 500
