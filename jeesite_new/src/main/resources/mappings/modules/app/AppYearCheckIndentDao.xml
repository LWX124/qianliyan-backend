<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.app.dao.AppYearCheckIndentDao">
	
	<!-- 查询数据
	<select id="findList" resultType="AppYearCheckIndent">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->
	<select id="findImgs" resultType="String">
		SELECT url FROM app_year_check_img WHERE app_year_check_id = #{sprayId}
	</select>


<!--	AppYearCheckIndent findByCheckNumber(String yearCheckNumber);-->
	<select id="findByCheckNumber" resultType="AppYearCheckIndent">
		SELECT * FROM app_year_check_indent WHERE year_check_number = #{yearCheckNumber}
	</select>



</mapper>
