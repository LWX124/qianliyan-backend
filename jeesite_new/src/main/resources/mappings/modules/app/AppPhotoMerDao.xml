<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.app.dao.AppPhotoMerDao">
	
	<!-- 查询数据
	<select id="findList" resultType="AppPhotoMer">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->
<!--	ArrayList<Integer> findAllImgs(String id);-->
	<select id="findAllImgs" resultType="Integer">

		SELECT id FROM app_photo_mer WHERE up_id = #{id}

	</select>


<!--	void insertNew(AppPhotoMer appPhotoMer);-->
	<insert id="insertNew">
		insert into app_photo_mer (url,`index`,up_id,create_time,update_time)
		value (#{url},#{index},#{upId},#{createTime},#{updateTime})
	</insert>


</mapper>