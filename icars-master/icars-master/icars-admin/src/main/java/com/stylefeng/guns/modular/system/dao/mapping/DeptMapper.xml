<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stylefeng.guns.modular.system.dao.DeptMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.stylefeng.guns.modular.system.model.Dept">
        <id column="id" property="id"/>
        <result column="num" property="num"/>
        <result column="pid" property="pid"/>
        <result column="pids" property="pids"/>
        <result column="simplename" property="simplename"/>
        <result column="fullname" property="fullname"/>
        <result column="tips" property="tips"/>
        <result column="address" property="address"/>
        <result column="imgUrls" property="imgUrls"/>
        <result column="synopsis" property="synopsis"/>
        <result column="companyids" property="companyids"/>
        <result column="scale_for_emp" property="scale_for_emp"/>
        <result column="scale_for_company" property="scale_for_company"/>
        <result column="version" property="version"/>
    </resultMap>

    <select id="tree" resultType="com.stylefeng.guns.core.node.ZTreeNode">
		select id,pid as pId,simplename as name,
		(
		CASE
		WHEN (pId = 0 OR pId IS NULL) THEN
		'true'
		ELSE
		'false'
		END
		) as isOpen from sys_dept
	</select>

    <select id="list" resultType="map">
        select * from sys_dept
        <if test="condition != null and condition != ''">
            where simplename like CONCAT('%',#{condition},'%') or fullname like CONCAT('%',#{condition},'%') or tips like CONCAT('%',#{condition},'%')
        </if>
        order by num ASC
    </select>

    <select id="listFours" resultType="map">
        select * from sys_dept
        <where>
            lat is not null and lng is not null
            <if test="fullName != null and fullName != ''">
                and (simplename like CONCAT('%',#{fullName},'%') or fullname like CONCAT('%',#{fullName},'%'))
            </if>
            <if test="tips != null and tips != ''">
                and tips like CONCAT('%',#{tips},'%')
            </if>
        </where>
        order by num ASC
    </select>


    <select id="selectPageByCondition" resultType="com.stylefeng.guns.modular.system.model.Dept">
        select * from sys_dept
        <where>
            <if test="dict != null and dict != ''">
                and address like CONCAT('%',#{dict},'%')
            </if>
            <if test="qcpp != null and qcpp != ''">
                and (tips like CONCAT('%',#{qcpp},'%') or fullname like CONCAT('%',#{qcpp},'%'))
            </if>
            and num is null
        </where>
    </select>


</mapper>
