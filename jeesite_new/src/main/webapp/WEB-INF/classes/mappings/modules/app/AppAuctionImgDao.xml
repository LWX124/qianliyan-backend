<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.app.dao.AppAuctionImgDao">

    <update id="updateImgOne">
		update app_auction_img SET url = #{imgSrc} WHERE id = #{imgId}
	</update>

    <!-- 查询数据
    <select id="findList" resultType="AppAuctionImg">
        SELECT ${sqlMap.column.toSql()}
        FROM ${sqlMap.table.toSql()}
        <where>
            ${sqlMap.where.toSql()}
        </where>
        ORDER BY ${sqlMap.order.toSql()}
    </select> -->
	<select id="findAllImgs" resultType="AppAuctionImg">
		SELECT * FROM app_auction_img a WHERE car_id = #{carId}
	</select>
</mapper>