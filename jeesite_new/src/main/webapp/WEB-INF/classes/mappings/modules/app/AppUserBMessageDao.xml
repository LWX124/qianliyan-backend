<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.app.dao.AppUserBMessageDao">
	
	<!-- 查询数据
	<select id="findList" resultType="AppUserBMessage">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->

<!--	AppUserBMessage findEmployee(String userBid, int type);-->
	<select id="findEmployee" resultType="AppUserBMessage">
		SELECT * FROM app_user_b_message WHERE wrok_type = #{type} AND user_b_id = #{userBid}
	</select>

</mapper>