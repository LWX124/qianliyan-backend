<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.app.dao.AppMessageCarDao">
	
	<!-- 查询数据
	<select id="findList" resultType="AppMessageCar">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->
<!--	AppMessageCar findPushBill(String id);	-->
	<select id="findPushBill" resultType="AppMessageCar">
		SELECT * FROM app_message_car WHERE mess_id = #{id}
	</select>
	
</mapper>