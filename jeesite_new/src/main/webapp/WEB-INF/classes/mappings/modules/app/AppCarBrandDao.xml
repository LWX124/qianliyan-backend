<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.app.dao.AppCarBrandDao">
	
	<!-- 查询数据
	<select id="findList" resultType="AppCarBrand">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->

	<select id="findList4Like" parameterType="string" resultType="AppCarBrand">
		select t1.id,t1.name,t1.pic_url from app_car_brand t1
		where t1.name like CONCAT('%',#{name},'%')
	</select>

	<!-- findCarBrandsById -->
	<select id="findCarBrandsById" resultType="AppCarBrand">
		SELECT DISTINCT id, name, pic_url picUrl FROM app_car_brand
		<where>
			id in
			<foreach item="item" collection="list" separator="," open="(" close=")" index="">
				#{item}
			</foreach>
		</where>
	</select>
</mapper>