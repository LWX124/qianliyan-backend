<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cheji.web.modular.mapper.AppSendUrlMapper">


<!--    String findMapImg(Integer id);-->
    <select id="findMapImg" resultType="String">
        SELECT url FROM app_send_url WHERE  send_id = #{id}   AND type =  1 AND `index` = 2

    </select>


<!--    String[] findAccidentImg(Integer id);-->
    <select id="findAccidentImg" resultType="String">
        SELECT url FROM app_send_url WHERE  send_id = #{id} AND type = 1 AND  source = 2
    </select>

<!--    String findCheckImg(Integer id);-->
    <select id="findCheckImg" resultType="String">
         SELECT url FROM app_send_url WHERE  send_id = #{id} AND type = 2 AND  source = 1  LIMIT 0,1
    </select>

</mapper>
