<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.app.dao.AppUserBehaviorDao">
	
	<!-- 查询数据
	<select id="findList" resultType="AppUserBehavior">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->

	<!--AppUserBehavior selectByUserId(String userId);-->
	<select id="selectByUserId" resultType="AppUserBehavior">
			SELECT * FROM app_user_behavior WHERE user_id = #{userId}
	</select>


<!--	List<AppUserBehavior> allselect(Integer pageoffset);-->
	<select id="allselect" resultType="AppUserBehavior">
		select * from app_user_behavior LIMIT #{pageoffset} ,20
	</select>
</mapper>
